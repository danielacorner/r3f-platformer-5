import { Vector3 } from 'three';

export const GRAVITY = new Vector3(0, -20, 0);
export const MAX_SPEED = 15;
export const SEEK_FORCE = 40;
export const HIT_RADIUS = 0.5;
export const HORIZONTAL_SEEK_HEIGHT = 2;
